'use strict'


/* global describe, it */
const simpleChangelog = require('.')

describe('simple-changelog', () => {
  it('write this test', () => {
    console.assert(simpleChangelog, 'should export something')
  })
})
